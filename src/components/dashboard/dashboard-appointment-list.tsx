'use client';
import { CalendarDays, Clock3, UserRound } from 'lucide-react';
import { AppointmentStatusBadge } from '@/components/appointments/appointment-status-badge';
import { useLanguage } from '@/lib/i18n/language-provider';
import type { DashboardAppointment } from '@/types/dashboard';
export function DashboardAppointmentList({ appointments }: { appointments: DashboardAppointment[] }) { const { locale, copy } = useLanguage(); if (!appointments.length) return <p className="rounded-[1.5rem] border border-dashed border-violet-200 bg-violet-50/55 p-8 text-center text-sm font-semibold text-[var(--muted)]">{copy.dashboard.noAppointments}</p>; return <div className="grid gap-4">{appointments.map((appointment) => <article key={appointment.id} className="rounded-[1.5rem] border border-[var(--line)] bg-white p-5 shadow-[0_10px_26px_rgba(37,42,92,.06)] sm:p-6"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-lg font-black">{appointment.clientName[locale]}</p><p className="mt-1 text-sm font-bold text-[var(--brand)]">{appointment.serviceName[locale]}</p></div><AppointmentStatusBadge status={appointment.status} /></div><div className="mt-4 grid gap-3 border-t border-[var(--line)] pt-4 text-sm font-semibold text-[#536383] sm:grid-cols-3"><span className="inline-flex items-center gap-2"><Clock3 size={16} className="text-[var(--brand)]" />{appointment.time}</span><span className="inline-flex items-center gap-2"><UserRound size={16} className="text-[var(--brand)]" />{appointment.employeeName[locale]}</span><span className="inline-flex items-center gap-2"><CalendarDays size={16} className="text-[var(--brand)]" />{appointment.clientContact}</span></div></article>)}</div>; }
