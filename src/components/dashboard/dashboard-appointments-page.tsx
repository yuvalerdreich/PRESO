'use client';
import { useMemo, useState } from 'react';
import { DashboardAppointmentList } from '@/components/dashboard/dashboard-appointment-list';
import { DashboardShell } from '@/components/dashboard/dashboard-shell';
import { useLanguage } from '@/lib/i18n/language-provider';
import type { DashboardAppointment, DashboardBusiness, DashboardEmployee } from '@/types/dashboard';
export function DashboardAppointmentsPage({ business, employees, appointments }: { business: DashboardBusiness; employees: DashboardEmployee[]; appointments: DashboardAppointment[] }) { const { locale, copy } = useLanguage(); const dates = [...new Set(appointments.map((appointment) => appointment.dateKey))]; const [date, setDate] = useState(dates[0] ?? ''); const [employee, setEmployee] = useState(''); const filtered = useMemo(() => appointments.filter((appointment) => appointment.dateKey === date && (!employee || appointment.employeeId === employee)), [appointments, date, employee]); return <DashboardShell business={business} active="appointments"><section className="mt-7"><p className="text-sm font-bold text-[var(--brand)]">{copy.dashboard.appointments}</p><h2 className="mt-1 text-2xl font-black tracking-tight sm:text-3xl">{copy.dashboard.detailsTitle}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">{copy.dashboard.detailsDescription}</p><div className="mt-6 grid gap-4 rounded-[1.5rem] border border-[var(--line)] bg-white p-4 sm:grid-cols-2"><label className="text-sm font-bold">{copy.dashboard.date}<select value={date} onChange={(event) => setDate(event.target.value)} className="mt-1.5 w-full rounded-xl border border-[var(--line)] bg-[#faf9ff] px-3 py-2.5 text-sm focus:outline-2 focus:outline-violet-200">{dates.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="text-sm font-bold">{copy.dashboard.employee}<select value={employee} onChange={(event) => setEmployee(event.target.value)} className="mt-1.5 w-full rounded-xl border border-[var(--line)] bg-[#faf9ff] px-3 py-2.5 text-sm focus:outline-2 focus:outline-violet-200"><option value="">{copy.dashboard.allEmployees}</option>{employees.map((item) => <option key={item.id} value={item.id}>{item.name[locale]}</option>)}</select></label></div><div className="mt-6"><DashboardAppointmentList appointments={filtered} /></div></section></DashboardShell>; }
